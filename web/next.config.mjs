/** @type {import('next').NextConfig} */
const nextConfig = {
  i18n: {
    locales: ['en', 'fr', 'de', 'ru', 'it', 'es', 'pl', 'uk', 'ro', 'nl', 'bg'],
    defaultLocale: 'en',
  },
};

export default nextConfig;
