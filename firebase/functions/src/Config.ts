import { ConfigType } from '../../../types/internal/ConfigType';

const Config: ConfigType = {
  defaultCurrency: 'EUR',
  supportedCurrencies: ['USD', 'EUR', 'BGN', 'AED'],
};

export default Config;
